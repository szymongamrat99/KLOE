#ifndef PLANE_INTERSECTION_H
#define PLANE_INTERSECTION_H

#include "TMath.h"

void plane_intersection(Float_t *, Float_t *,
                        Float_t *, Float_t *,
                        Float_t *);

void plane_intersection(Double_t *, Double_t *,
                        Double_t *, Double_t *,
                        Double_t *);

#endif